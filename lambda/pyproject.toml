[project]
name = "llm-gateway"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.43.93",
]

[dependency-groups]
dev = [
    "boto3-stubs[bedrock-runtime,dynamodb,lambda]>=1.43.93",
    "moto>=5.2.3",
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
